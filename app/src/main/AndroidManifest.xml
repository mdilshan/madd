<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.madd">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".HotelDetails"></activity>

        <activity android:name=".HotelSeeAll" />
        <activity android:name=".HotelMainPage" />
        <activity android:name=".ViewHotel" />
        <activity android:name=".HotelContentList">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddNewHotel" />

        <activity android:name=".PlaceActivity"></activity>
        <activity android:name=".PlaceSeeAll" />
        <activity android:name=".EditPlaces" />
        <activity android:name=".DetailsActivity" />
        <activity android:name=".AddPlaces" />
        <activity android:name=".GuideJoin"></activity>
        <activity android:name=".GuideEdit"></activity>
        <activity android:name=".GuideDetails" />
        <activity android:name=".GuideHome" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>